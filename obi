import SoundButton from "../components/SoundButton";

export default function AppView({ app, exit }) {
  return (
    <div style={{ padding: "20px" }}>
      <h1>📱 {app.name}</h1>

      <SoundButton label="Sample Sound" sound="/sounds/click.mp3" />

      <br /><br />
      <button onClick={exit}>🔙 Back to Store</button>
    </div>
  );
}
